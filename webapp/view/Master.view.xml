<mvc:View
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
	xmlns:f="sap.ui.layout.form">
	<Page title="Product 1 Info"
		binding="{/Products(1)}">
		<content>
			<List id="productList" 
				class="sapuiResponsiveMargin"
				width="auto"
				items="{path:'/Products',
				sorter:{path:'Category/CategoryName', descending: false, group: true},
				parameters:{'expand': 'Category'}}"
				growing = "true"
				growingThreshold="5">
				<headerToolbar>
					<Toolbar>
						<Title text="Products in Stock" />
						<ToolbarSpacer />
						<Button icon="sap-icon://sort" press="onSortPoductName"/>
						<SearchField width="50%" search="inFilterProducts" />
					</Toolbar>
				</headerToolbar>
				<items>
					<ObjectListItem
						title="{UnitsInStock} x {ProductName}"
						number="{
							parts: [{path:'UnitPrice'},'EUR'],
							type: 'sap.ui.model.type.Currency',
							formatOptions:{showMeasure:false}}"
						numberUnit="EUR">
						<attributes>
							<ObjectAttribute text="{Category/CategoryName}"/>
						</attributes>
					</ObjectListItem>
				</items>
			</List>
		</content>
	</Page>
</mvc:View>